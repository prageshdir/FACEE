# ──────────────────────────────────────────────
# Face & Gait Recognition — Requirements
# Python 3.8 – 3.11 recommended
# ──────────────────────────────────────────────

# Core computer vision
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0      # extra algorithms (MOG2, etc.)

# Face recognition (dlib-based, very accurate)
face-recognition>=1.3.0
face-recognition-models>=0.3.0

# Deep learning (used by face_recognition internally; keep lightweight)
dlib>=19.24.0

# GUI image display in Tkinter
Pillow>=10.0.0

# Numerical computing
numpy>=1.24.0
scipy>=1.10.0

# Machine learning utilities
scikit-learn>=1.3.0

# (Optional) MediaPipe for pose estimation / body tracking
# Uncomment if you want pose-based gait analysis:
# mediapipe>=0.10.0

# Dataset management
kaggle>=1.5.16       # for downloading datasets via CLI

# Utilities
tqdm>=4.65.0         # progress bars during dataset loading
