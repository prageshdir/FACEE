import 'package:get_it/get_it.dart';

// Business feature repositories are registered here as they are built.
// Each feature module will call sl.registerLazySingleton<IXxxRepository>(...)
void registerRepositoryModule(GetIt sl) {
  // Repositories registered here when feature modules are added.
}
